
describe('ProgressBar', function() {

  beforeEach(module('sport.ng'))

  // beforeEach(inject(function(ResourceManager) {
  //   api = ResourceManager(resourceConfig)
  //   resSpy.calls.reset()
  // }))

  it('should use percentage if passed in', function() {
    expect(false).toBe(true)
  })

  it('should calculate percentage if not passed in', function() {
    expect(false).toBe(true)
  })

})